const mongoose = require('mongoose');

let ArtistSchema = require('../schemas/ArtistSchema');

class ArtistDAO {
    constructor(id) {
        this._id = id;
    }

    retrieve() {
        
    }

    save() {

    }
}